# Rucoy Bot Project

## Установка
1. Установите Python 3.8+
2. `pip install -r requirements.txt`
3. Настройте `config.json` (пути к эмулятору)

## Запуск
`python main.py`

## Сборка в EXE
`pyinstaller --noconsole --onefile --name "RucoyBot" main.py`